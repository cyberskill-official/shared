---
name: 🧪 CI - Check
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions: {}
jobs:
  check:
    name: 🧪 Check (Node ${{ matrix.NODE_VERSION }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: contains(github.event.head_commit.message, 'from cyberskill-official/release') == false
    strategy:
      fail-fast: false
      matrix:
        NODE_VERSION: [24.16.0]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7  # v6.0.2
        with:
          persist-credentials: false

      - name: Env & Deps
        # keep @main, do not change
        uses: cyberskill-official/.github/actions/env-deps@main # main
        with:
          NODE_VERSION: ${{ matrix.NODE_VERSION }}

      - name: Build Cache
        uses: actions/cache@v6  # v5.0.4
        with:
          path: |
            dist
            node_modules/.cache
          key: ${{ runner.os }}-build-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-build-

      - name: Security Audit
        run: pnpm audit --audit-level=moderate --ignore-registry-errors

      - name: License Compliance
        run: pnpm exec license-checker --summary --failOn "GPL;AGPL;LGPL;SSPL"

      - name: Lint
        # keep @main, do not change
        uses: cyberskill-official/.github/actions/lint@main # main

      - name: Build
        run: pnpm build

      - name: Validate Exports
        run: npx tsx scripts/validate-exports.ts

      - name: Typecheck
        run: pnpm typecheck

      - name: Test with Coverage
        run: pnpm test:unit -- --coverage

      - name: Test E2E
        run: pnpm test:e2e
