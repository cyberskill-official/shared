{
    "_comment": "Stage-0 PreToolUse policy for @cyberskill/shared. Defaults already block destructive commands and secret reads/exfil (.env is covered). Here we additionally make test files read-only during agent runs so a model can't edit a test to make code 'pass' (ImpossibleBench). Reads stay allowed so tests can run.",
    "deny_write_globs": [
        "**/*.test.ts",
        "**/*.test.tsx",
        "**/*.spec.ts",
        "**/*.spec.tsx",
        "**/__tests__/**",
        "tests/**",
        "e2e/**"
    ]
}
